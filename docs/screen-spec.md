# 画面仕様

サービス共通の色、文字、余白、ボタン、カード、状態表現は `docs/design-system.md` を参照する。本書では各画面固有の目的、項目、挙動を定義する。

## 1. ルーティング

| ルート | 画面 | 役割 |
| --- | --- | --- |
| `/` | トップページ | サービス説明と場面選択への入口 |
| `/categories` | 場面選択 | 5カテゴリから場面を選ぶ |
| `/categories/[categoryId]` | カテゴリ別問題一覧 | 選択したカテゴリの5問題を表示する |
| `/problems` | 互換リダイレクト | `/categories/train-station` へ移動する |
| `/problems/[slug]` | 問題詳細 | 状況説明、フレーズ、スタッフ向け表示を提供する |

駅員向け全画面表示は独立ルートを作らず、問題詳細内のモーダルとする。

## 2. 画面遷移

```text
トップページ
  └─ Find a phrase → 場面選択
       └─ 5カテゴリのいずれか → カテゴリ別問題一覧
            └─ 5カテゴリすべての問題カード → 問題詳細
            ├─ Back → 問題一覧
            └─ Show to staff → 同一画面でモーダル表示
                                  └─ Close → 問題詳細
```

## 3. トップページ

### 目的

利用者にサービスの用途と制約を短く伝え、場面選択へ迷わず進ませる。

### 表示項目

- サービス名 `Japan Travel Phrases`
- 見出し `Need help in Japan? Say it in Japanese.`
- 短い英語説明 `Pick a problem. Get the phrase.`
- 主ボタン `Find a phrase`
- 緊急サービスではないことを示す `Emergency? Ask staff or call 119.`

### 操作

- `Find a phrase` で `/categories` へ移動する。

## 4. 場面選択

### 目的

利用者が日本語フレーズを必要としている場面を短時間で選べるようにする。

### 表示項目

- 見出し `Where do you need help?`
- 英語検索欄（可視見出しや説明文は表示せず、支援技術向けラベルを設定する）
- `Train & Station`、`Airport`、`Restaurant`、`Hotel`、`Shopping` の5カテゴリ
- 各カテゴリの短い英語説明

### 操作

- 検索欄へ入力すると、問題名、短い説明、状況説明、フレーズ英訳、カテゴリ名を対象に25問題を即時絞り込みする。
- 検索は英語のみを対象とし、大文字・小文字を区別しない。空白で区切った複数語は、すべて含む問題だけを表示する。
- 入力が空の場合は検索結果を表示せず、5カテゴリの選択を主導線として維持する。
- 検索結果にはカテゴリ名、問題名、短い説明を表示し、選択すると `/problems/{slug}` へ直接移動する。
- 一致件数を支援技術へ通知し、0件の場合は `No matching problems. Try another word or choose a scene below.` を表示する。
- `Clear` で検索語と結果を消去する。検索語は保存せず、URLにも反映しない。
- 検索語が空の場合は件数表示用の空白を確保せず、検索欄と `Browse by scene` の間を簡潔に保つ。
- 5カテゴリすべてをカード全体のリンクとし、`/categories/{categoryId}` へ移動する。
- `Back to home` で `/` へ戻る。

## 5. カテゴリ別問題一覧

### 目的

利用者が選択した場面の困りごとを5候補から短時間で確認できるようにする。

### 表示項目

- 選択中のカテゴリ名
- 見出し `What’s gone wrong?`
- 5件の問題カード
- 各カードに英語の問題名と短い英語説明

カード全体を操作可能にする場合も、リンクであることが支援技術と視覚の両方で分かるようにする。カード文言は `docs/content-spec.md` のデータを使用する。

### 操作

- 5カテゴリすべての問題カードを選ぶと `/problems/{slug}` へ移動する。

## 6. 問題詳細

### 目的

選択した問題の状況を確認し、解決に必要なフレーズを読んで、スタッフへ見せられるようにする。

### 表示項目

- `Back` ボタンまたはリンク（遷移先は選択中カテゴリの問題一覧）
- 問題名
- 状況の英語説明
- 3〜5件のフレーズカード

各フレーズカードには次を表示する。

- 日本語（カード内で最も目立つ）
- ローマ字
- 英訳
- `Show to staff` ボタン

### 食物アレルギー詳細の追加仕様

- `food-allergy` では、フレーズより前にアレルゲン選択欄を表示する。
- そば、卵、乳、落花生、小麦、えび、かにを候補として表示し、候補外は自由入力できる。
- 選択または入力したアレルゲン名で、フレーズ内の `〇〇`、ローマ字内の `〇〇`、英訳内の `[allergen]` を置換する。
- 自由入力値は翻訳せず、日本語、ローマ字、英訳へ同じ値を使用する。このため、スタッフが理解できる名称を入力するよう英語で案内する。
- アレルゲン未指定時は `Show to staff` を無効にし、`Select an allergen first` と表示する。
- 成分確認が必要であり、本サービスが安全を保証しないことを英語で明示する。
- 入力値はブラウザ内の表示状態だけで扱い、保存、送信、API呼び出しを行わない。

## 7. 駅員向け全画面モーダル

### 表示項目

- 大きな日本語
- 小さな英訳
- `Close` ボタン

### 挙動

- `Show to staff` を押したフレーズの内容を表示する。
- 画面端に背景が見える余白を設けた大きな中央パネルとし、モーダルであることを視覚的に伝えながら、日本語を最優先で読みやすくする。
- 開いた直後は `Close` にフォーカスする。
- フォーカスをモーダル内に閉じ込め、閉じたら起点の `Show to staff` へ戻す。
- 文字付きの `Close` またはキーボードの `Escape` で閉じる。誤操作防止のため、背景をタップ・クリックしても閉じない。
- モーダル表示中は背面コンテンツを操作できないようにする。
- モーダル表示中は背面ページのスクロールを止める。

## 8. モバイル対応

- 基準幅は375px前後とする。
- 横スクロールを発生させない。
- 主要ボタンは指で押しやすい寸法と間隔を確保する。
- 日本語は改行されても意味と可読性が損なわれないサイズ・行間にする。
- コンテンツ幅を制限し、より広い画面でも読み順と操作順を維持する。
- 端末のセーフエリアを考慮する必要性は実機確認で判断する。

## 9. アクセシビリティ

- 見出し階層を順序どおりにする。
- 主要操作には英語の可視ラベルを付け、アイコンだけにしない。
- リンクとボタンを目的に応じて使い分ける。
- キーボードだけで移動とモーダルの開閉を行えるようにする。
- フォーカス位置を視覚的に示す。
- 色以外の文言・形状でも状態を伝える。
- 十分な文字コントラストを確保する。

## 10. エラー時の表示

- 個別コンテンツの不備でページ全体を操作不能にしない。
- 静的データの欠落はビルド時の型・検証で検出することを優先する。

## 11. 存在しないカテゴリとslug

- 静的データに一致しない `/categories/[categoryId]` は Next.js の `notFound()` を使用して404として扱い、`Choose a scene` から `/categories` へ戻れるようにする。
- 静的データに一致しない `/problems/[slug]` は Next.js の `notFound()` を使用して404として扱い、`Choose a scene` から `/categories` へ戻れるようにする。
- どちらも共通デザインを使用し、英語でページが見つからないことと次の操作を簡潔に伝える。404画面はMVPの新規ルートには数えない。

## 12. ローディング

MVPはローカル静的データのみを使用し、実行時データ取得を行わないため、専用ローディング画面やスケルトンは不要とする。

## 13. 受け入れ条件

- 5種類のルートが仕様どおりに表示・遷移する。
- 場面選択に5カテゴリが表示され、すべて対応する問題一覧へ遷移する。
- 場面選択で英語検索ができ、問題名・説明・フレーズ英訳の一致結果から問題詳細へ移動できる。
- 検索0件、`Clear`、大文字・小文字の違い、複数語入力が仕様どおり動作する。
- 各カテゴリの一覧に対象5問題だけが表示される。
- 25問題すべてが操作可能で、対応する詳細へ遷移する。
- 全有効slugで対応する問題と3件のフレーズが表示される。
- 無効なslugが404になる。
- 無効なカテゴリIDが404になり、場面選択へ戻れる。
- 全75フレーズで日本語、ローマ字、英訳、`Show to staff` が確認できる。
- `food-allergy` で候補選択と自由入力の両方がフレーズおよびモーダルへ反映され、未指定時はスタッフ表示を開けない。
- モーダルが選択フレーズを表示し、キーボードとタッチの両方で閉じられる。
- 375px前後で横スクロール、重なり、操作不能な要素がない。
- トップから対象フレーズへ2〜3操作以内で到達できる。
- 色やアイコンだけに依存する主要操作・状態がない。
